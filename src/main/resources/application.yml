spring:
  datasource:
    url: jdbc:mysql://localhost:3306/antonio_prova
    username: root
    password: password
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
  jackson:
    serialization:
      fail-on-empty-beans: false
